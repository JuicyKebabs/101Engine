# ActorImprint implementation record

## Contract and workflow

- Source: [Final Design](https://app.notion.com/p/3cfdef1c864e803c9484d470d9a439ad), last edited 2026-09-12 02:37:26 UTC.
- Tickets: [ET-08 / #46](https://github.com/JuicyKebabs/101Engine/issues/46) through [ET-18 / #56](https://github.com/JuicyKebabs/101Engine/issues/56), plus the basic Document-tab follow-up [Issue #68](https://github.com/JuicyKebabs/101Engine/issues/68).
- The 2026-09-12 implementation instruction authorizes sequential implementation without intermediate Owner Review. Each ticket must pass its required verification before the next ticket starts. Final acceptance remains with the user.
- Material unresolved decisions still require the user's decision. Cross-Ticket Architecture Guardrails and AI Engineering Guardrails apply throughout.
- Current ticket: Issue #68 completion candidate. ET-08 through ET-18 are also completion candidates; final acceptance remains with the user.
- Do not infer completion from this record: implementation and test evidence must be inspected at each completion audit.

## Baseline verified on 2026-09-12

- Branch: `master`; starting commit: `39a9b73`.
- Pre-existing untracked user file: `docs/CodingStyleReference.md`; leave intact.
- Visual Studio 2022 CMake: `C:/Program Files/Microsoft Visual Studio/2022/Community/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/cmake.exe`.
- `cmake --build build --config Debug --parallel 4`: passed, including 101Framework, GameCode, 101Game, 101Editor, and test targets.
- The first sandboxed build failed while MSBuild inspected the user Windows SDK directory. The approved build outside the sandbox passed.
- Existing MSB8064 warnings report a missing dependency path for Game/GameCode; no change was made to silence these baseline warnings.
- `ctest --test-dir build -C Debug --output-on-failure`: 31/31 passed, 1.94 seconds.

## Ticket status and verification index

| Ticket | Responsibility | Status | Required verification focus |
| --- | --- | --- | --- |
| ET-08 / #46 | Immutable asset v1 and normalized defaults | Completion candidate: Debug build and 33/33 tests pass | Strict schema, one closed root, shared ID namespace, references, normalization and deterministic round-trip; prior Reflection tests and Debug build |
| ET-09 / #47 | Catalog, AssetChange, System pool and handles | Completion candidate: Debug build and 34/34 tests pass | Lazy load, duplicate/missing/type failures, stale handles, event queue; ET-08 and AssetReference regression |
| ET-10 / #48 | Materialization and scene registry | Completion candidate: Debug build and 36/36 tests pass | New/restored GUIDs, multiple instances, internal references, external parents, failure leaves no observable partial state |
| ET-11 / #49 | Structural policy and lifetime | Completion candidate: Debug build and 37/37 tests pass | All public mutation paths, instance destroy, deferred GC and reverse-index lifetime, ordinary actor regression |
| ET-12 / #50 | Overrides and instance records | Completion candidate: Debug build and 39/39 tests pass | Atomic arrays/references, null, pointer conflicts, exact normalized comparison, revision migration, full-object failure |
| ET-13 / #51 | Scene v4 and candidate load | Completion candidate: Debug build and 41/41 tests pass | Whole-scene identity/provenance, order independence, v3 compatibility/v2 rejection, caller state preservation, deterministic save |
| ET-14 / #52 | Definition reload transaction | Completion candidate: Debug build and 42/42 tests pass | All live scenes commit together, failure preserves definition and editor state, GUID migration, Missing and same-revision behavior |
| ET-15 / #53 | Editor Document foundation | Completion candidate: Debug build and 43/43 tests pass | Per-document history, selection, viewport, dirty state; normal Scene workflows and Play policy |
| ET-16 / #54 | Imprint editing document | Completion candidate: Debug build and 44/44 tests pass | WorkingScene/ObjectMap undo, stable IDs, closed-root policy, validated atomic save and reload |
| ET-17 / #55 | Asset panel and editor workflows | Completion candidate: Debug build and 45/45 tests pass | Transactional asset creation, deletion constraints, GUID/type drag payload, instantiate undo/redo, typed picker |
| ET-18 / #56 | Integration and completion audit | Completion candidate: Debug build and 46/46 tests pass | Requirement-to-code-to-test traceability, full E2E/failure injection, complete Debug/test run, Editor smoke and performance observations |
| Follow-up / #68 | Basic Document tabs and ActorImprint asset operation routes | Completion candidate: Debug build and 46/46 tests pass | Central tabs, dirty close/exit decisions, managed asset directory, panel/MenuBar operations and production-boundary UI intent tests |

Each row is an index into the original ticket, not a replacement for its full acceptance criteria.

## ET-08 decisions approved by the user (2026-09-12)

1. DefinitionRevision: a dedicated type using the existing nonzero Guid representation and canonical Guid string. A successful save of changed normalized content issues a new Guid; an equivalent save retains the revision. The user approved this over a content-derived hash.
2. Actor property metadata: use the existing TypeMetadataBuilder for Actor name, tag and is_active with Actor setters. Reuse it for Imprint defaults and subsequent overrides; preserve existing Scene record placement and Actor Inspector UI. Final Design calls for Actor metadata, and #46 explicitly asks for Owner review when adding it.
3. Imprint ActorReference representation: null, or a typed object with type=ActorReference and scope=local/localObjectId, or scope=scene/actorGuid. Use the existing codec interface and keep runtime ActorReference GUID-based. Asset definitions reject scene scope. Instance overrides may use it in ET-12. Existing Scene v3 and AssetReference GUID forms remain compatible. The user approved the proposed fields.

The user's instruction "ET-08について、全て推奨設計通りに進めてください" approves all three choices. Dependent ET-08 implementation may proceed.

## ET-08 independent object graph checkpoint (2026-09-12)

- Implemented `FrameWork/src/Engine/ActorImprint/LocalObjectId.h` using the Final Design's uint64 identity rule.
- Implemented `Detail/ActorImprintObjectGraph.h/.cpp`, an internal asset-reader phase that only validates IDs and Actor parent relationships. It does not validate or choose Revision, property metadata, reference representation, component type or complete asset schema. It does not create runtime Actors or register anything in Scene/Pool.
- Checks shared Actor/Component identity, nonzero unsigned range, next ID beyond all present IDs, exactly the declared root, Actor-only parent/root targets, missing/external parents and cycles. The graph is temporary validation state, not an additional owned runtime hierarchy.
- Iterative visitation handles deep input without recursive stack growth. Input array indices are retained in diagnostic JSON Pointers; parents may occur after children.
- `Tests/ActorImprintObjectGraphTests.cpp` exercises valid and malformed graphs, cross-kind duplicate IDs, unsigned precision and overflow, null/missing/type failures, disconnected cycles, order independence and an 8,192-Actor reverse-order chain. Its minimal graph fixtures deliberately are not full assets.
- Reconfigured the existing CMake build to discover new Framework source. Full Debug build passed, including 101Editor and the new test executable. MSB8064 and third-party DirectXTex missing-PDB LNK4099 warnings remain.
- Full CTest run: 32/32 passed, 2.13 seconds. `git diff --check` passed for tracked changes.
- This is a narrow, independently testable checkpoint, not the promised full Asset deserialization pilot and not ET-08 completion. No broad migration, new subsystem registry, runtime graph or generic validation framework was introduced. The full reader must call this phase before publishing a definition and test it through that production entry point.
- At this historical checkpoint the three design questions were unanswered. The approval and subsequent full-reader checkpoint below supersede that state.

## ET-08 pilot and test plan

The first pilot will deserialize and serialize one Actor with a real registered Transform and complete reflected properties. It must demonstrate an immutable result, deterministic output, float normalization, position-bearing diagnostics and no partial result before expanding to hierarchies and references.

- Validate allowed/required/null/type/range rules at asset, actor and component levels.
- Validate Actor and Component LocalObjectIDs together: nonzero uint64, uniqueness, next ID beyond every used ID, no wraparound.
- Validate root identity, exactly one root, existing actor parents, connectivity and cycles without dependence on input array order.
- Resolve stable component names through ComponentRegistry; use registered component cardinality and Transform-family policy.
- Normalize component property objects through existing Reflection Deserialize/Serialize; do not add per-component serializers.
- Exercise a Camera with internal ActorReference and a renderer with AssetReference after the reference contract is settled.
- Cover invalid references, unknown fields, incomplete properties, malformed numeric IDs, invalid component types and type invariants.
- Inspect the pilot's resulting API and responsibilities separately from test success before broadening it.

### ET-08 full reader pilot checkpoint

- After approval of the three ET-08 decisions, added immutable ActorImprint definitions, GUID-backed DefinitionRevision, Actor metadata using existing setters, the definition-local ActorReference codec, and the Asset Serializer/Deserializer.
- The reader validates the object graph, uses ComponentRegistry factories and Actor's existing cardinality/family policy on detached validation objects, and normalizes through Reflection. No Scene registration or attach occurs. Asset references use the existing AssetReferenceCodec and caller-provided catalog validation context.
- The complete one-Actor/Transform pilot built and passed `ActorImprintAssetTests` (2026-09-12, 0.43 seconds). It verified immutable construction, float precision normalization, byte-stable round-trip, positioned property failure and preservation of an existing definition after a separate failed read.
- Pilot review: callers supply JSON and an optional Asset validation context; they do not restate component schemas or mutation rules. The new concepts are the domain definition, its Revision, the two persistence directions and the required local-reference translation. There is no new component factory, property system or runtime Actor graph. The earlier object graph validator is now called by the production reader.
- This checkpoint precedes expansion to multiple components, references and malformed inputs. It is preserved as this record and the named test; no commit was requested or created.

## ET-08 completion review (2026-09-12)

- Scope delivered: immutable definitions, GUID-backed revision, shared uint64 LocalObjectIDs, [documented v1 schema](ActorImprintAssetV1.md), canonical sample asset, strict reader/writer, Actor metadata and local-reference translation through the existing codec interface.
- Validation evidence: the production reader tests complete graphs, forward internal references, same-type multiple Components, catalog-validated AssetReferences, built-in schemas, whole-Component Camera invariants, non-idempotent setters, malformed fields/IDs, duplicate JSON keys and file failures. Separate graph tests cover deep hierarchy and uint64 boundaries. No candidate Component attaches to a Scene.
- `cmake --build build --config Debug --parallel 4` passed for Framework, GameCode, Game, Editor and tests. `ctest --test-dir build -C Debug --output-on-failure`: 33/33 passed in 2.33 seconds. `git diff --check` passed. Baseline MSB8064 warnings remain; third-party PDB warnings appeared when Framework was relinked.
- Responsibility review: definition/schema ownership stays in ActorImprint; Component factory/policy stays in ComponentRegistry and Actor; Property values and invariants stay in Reflection/domain setters. Reflection's existing schema validator now supplies precise missing/unknown/nested paths rather than adding an Imprint-specific property validator. Actor setters and TagRegistry behavior are preserved, including automatic tag registration.
- Known consumers reviewed: ET-09 catalog validation and Slot ownership; ET-10 materialization/local identity; ET-12 normalized defaults and reference atomicity; ET-13 Scene embedding; ET-14 revision replacement; ET-16 snapshot/save. No Imprint dependency was added to generic Reflection or AssetManager in ET-08.
- New concepts are those required by the ticket (definition/revision/persistence and local-reference codec). Stable type names and runtime type information have distinct persistence/runtime roles. No second Property system, Component factory or runtime hierarchy was introduced. The representative pilot preceded expanded cases; no broad migration occurred.
- Limits/deferred: DOM input cannot detect duplicate keys already discarded by a parser; file Load detects them. Type metadata must outlive use of resolved types. Revision issuance during edited save, live Scene transactions, UI hierarchy policy and Asset catalog integration remain in their owning later tickets. ET-08 itself has no unresolved material choice and meets its acceptance criteria; no Issue was closed.

## ET-09 approved catalog failure policy

- The user chose whole-catalog failure on duplicate GUIDs or invalid existing `.meta`: retain the previous catalog and existing metadata identity. This supersedes the old overwrite/regenerate behavior in AssetManager.
- The new typed reference and System/Slot pilot passed before the catalog change expanded. Loaded definitions have no replacement API at this stage; ET-14 owns replacement with live Instance migration. Explicit unload ends a cached definition lifetime and increments Slot generation.
- Final Design explicitly assigns System ownership to Game App/EditorApp, with a non-owning EngineContext pointer and Scene-first teardown. GameCode unload must clear definitions after Scene destruction and before registry/DLL teardown.

## ET-09 completion review (2026-09-12)

- Delivered `AssetType::ActorImprint`, typed `AssetReference<ActorImprint>`, `.imprint` discovery, generic `AssetChange`, catalog `Refresh` / explicit `NotifyAssetChanged` / `TakePendingChanges`, System-owned Slots and GUID map, generation-checked handles, lazy load, Resolve, Unload and Clear.
- Pilot: one real `.imprint` discovered by the existing scanner, loaded through the existing catalog context and ET-08 reader, resolved repeatedly, unloaded and reloaded into a reused Slot. Build and `ActorImprintSystemTests` passed in 0.42 seconds before catalog/lifetime expansion. API review found no second scanner, metadata reader, asset resolver, component registry or actor pool.
- Catalog publication swaps all candidate indices, file observations and pending events together. Duplicate GUIDs, invalid existing metadata and scan errors retain the old catalog. MetaFile validates nonzero GUIDs and reports write failure. New-sidecar writes use Windows `CREATE_NEW` to reject an already-existing file at the actual open operation; see [CreateFileW](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilew). The scanner never replaces existing sidecars. A later I/O failure can leave a newly created sidecar on disk, which is retained as its stable identity; filesystem-wide rollback is not claimed.
- Scan observations use path, asset size/write time and metadata size/write time/GUID. Explicit notification forces a Modified candidate when timestamps are unchanged. Identical pending events are coalesced; distinct Added/Modified/Removed transitions are retained. No semantic Revision comparison, definition mutation, Imprint callback, or watcher thread is inside AssetManager. ET-14 will wire the Editor's safe update dispatch; Game performs no ongoing refresh/reload.
- Game/Editor own System and inject its non-owning pointer through EngineContext. Catalog failure aborts App initialization before Scene creation. Scene teardown precedes System teardown. Every GameCode unload/rollback path clears definitions before unregistering/unloading their runtime types. Borrowed definition pointers expire at unload/clear/System destruction; handles are scoped to one System. Exhausted generations retire their Slot instead of wrapping.
- Tests cover mixed asset types, automatic sidecar creation, explicit and observed changes, event coalescing, whole-catalog duplicate/malformed-meta failure, preservation of pending events, failed reinitialization, typed reference codec reuse, invalid/missing/type-mismatch loads, failed candidate isolation, cached definition immutability, forged/stale handles, Clear and restored files.
- Final full Debug build passed for Framework, GameCode, Game, Editor and tests. Full CTest: 34/34 passed in 2.34 seconds. `git diff --check` passed. Existing MSB8064, third-party missing-PDB LNK4099, and C4819 warnings in unchanged Renderer/CollisionSystem code remain.
- Architecture review: AssetManager remains the only catalog/scanner; MetaFile owns sidecar encoding; the existing AssetManagerAssetReferenceContext owns type-aware lookup validation; System owns definitions and Slots; App owns lifetime. The added file-state cache represents prior scan observations, not a second asset catalog. No generic pipeline or callback framework was added. The changes across App/Context/Resource/System are required by the explicit ownership contract, rather than per-Component mechanical migration.
- Deferred: Instance Registry and live-reference unload protection (ET-10/11), safe-point Editor event dispatch/Missing/reload transaction (ET-14), transactional two-file asset creation and deletion (ET-17). No loaded-definition replacement API is exposed before those guarantees exist. All ET-09 acceptance criteria are satisfied; no Issue is closed and no final acceptance is claimed.

## Cross-ticket findings to resolve in their owning work

### ET-10 active design boundary

- Issue #48 and its Notion page were read after ET-09 verification. The independent detached-candidate phase below is implemented; the Instance commit and Registry are not implemented yet.
- Pending Owner decision: `Component::OnAttach` returns void and sets its attached flag only after the virtual callback returns. Arbitrary callback side effects and exceptions have no general rollback contract. The proposed minimum contract makes attach a non-failing commit stage after all recoverable validation, and forbids reentrant Scene structure mutation during commit. The alternative is an explicit fallible prepare/commit lifecycle with broader Component migration. This decision also constrains ET-13 whole-Scene load and ET-14 all-live-Scene reload; it must not be chosen locally inside materialization.
- Independent investigation: use existing Actor exact-type/occurrence lookup for Component correspondence rather than long-lived raw pointers or new persistent Component GUIDs. Registry will own provenance and LocalObjectID correspondence only; ActorPool/Scene remain Actor/GUID/hierarchy owners. Scene/Registry teardown must release definition references before System unload; public Unload/Clear need live-reference protection when this registration exists.
- UI observation: existing ApplyUIHierarchyConstraints can replace Transform-family Components. Immutable Instance materialization must prevalidate the required transform kind and reject incompatible placement, while sharing the existing UI rule. Silently converting definition-owned structure would violate Final Design. A normal Transform root under a Canvas and a Screen-Space Canvas with a Transform rather than RectTransform are representative rejection tests.
- Planned representative materialization test: one real Actor/Transform instance, new versus restored GUID, Registry lookup and unchanged destination on invalid input. After that pilot and API review, expand to forward internal ActorReferences, multiple same-type Components, two independent Instances, external normal parents, invalid reference/UI cases, deep hierarchy and failure injection. Attach callbacks must observe the completed identity/hierarchy only at the approved commit stage.

### ET-10 detached candidate checkpoint (2026-09-12)

- Added `Detail/ActorImprintDetachedActors.h/.cpp`: one internal preparation phase for the future shared materialization path. It validates complete restored Actor GUID mapping (or generates new GUIDs), rejects duplicates and destination collisions before Component construction, creates all detached Actor shells, translates local ActorReference values through the ET-08 codec and applies defaults through existing Reflection/ComponentRegistry.
- The returned owning vector follows the immutable definition's Actor order. It is temporary candidate storage, not a second Actor pool, hierarchy or provenance registry. Definition structure is still the only source for parent and Component correspondence. ActorFactory and ComponentRegistry still own object construction; no per-Component serializer or lifecycle policy was added.
- This phase deliberately establishes no Scene ownership, ActorHandle, hierarchy, final reference resolution, attach, or Instance Registry. AssetReference GUIDs remain unresolved for the later complete validation/resolution stage. It is not sufficient to publish an Instance, and does not pre-empt the pending attach decision.
- Pilot: one real Actor/Transform candidate, valid new GUID, null Scene owner/Handle and unchanged destination. Build plus its focused test passed (0.48 seconds) before expanding cases. API review confirmed that the phase uses existing factories/codecs and returns ordinary owned Actor objects; it does not create a parallel runtime model.
- Expanded tests cover new/restored identity, malformed mapping, GUID conflicts, duplicate Component occurrences, per-candidate state and internal-reference isolation, Property/factory/exception/missing-registration failures, complete candidate destruction, unchanged existing Scene, and a 1,024-Actor reversed-input definition. A test-only call initially omitted ActorFactory's required InitDesc; it was corrected to the existing API before tests ran.
- Full Debug build passed including Game and Editor. Full CTest: 35/35 passed in 2.83 seconds. `git diff --check` passed. Existing build warnings remain. This is an independently verified ET-10 checkpoint, not ET-10 completion; later tickets have not started.
- The attach decision remains unanswered after this continuation. Independent candidate preparation and verification are complete. Subsequent Scene publication, lifecycle and Registry integration must use the approved transaction boundary rather than assume an answer.

- Current `GuidActorReferenceCodec::Deserialize` has no restore-context parameter and creates a GUID-based ActorReference. Local ID translation must fit the existing codec extension boundary without adding a second runtime reference model or leaking Imprint into generic Reflection.
- `SceneBase::ApplyUIHierarchyConstraints` currently calls `EnsureActorTransformKind`, which can replace Transform/RectTransform. Applying it unchanged to an Imprint can change definition-owned component structure and invalidate LocalObjectID correspondence. ET-10/11 and ET-16 must explicitly address this; no silent structural conversion is permitted.
- Existing `Component::OnAttach` is void and may register with Scene systems. ET-10/13 must establish where all fallible validation ends and how candidate lifetime contains attachment side effects.
- Existing ActorSubtreeRestorer stages detached actors but later registers into the supplied Scene and rolls back. It is reference code, not proof of the ET-13 whole-scene candidate guarantee.
- EditorApp currently clears History/Selection and destroys the old Scene before loading. ET-13 must migrate callers, not merely introduce a new loader entry point.
- GameCode unload removes ComponentRegistry entries. Loaded Imprint type information must not outlive the module that defines it. Revisit this at System lifetime and hot-reload integration boundaries.
- ActorImprint assets must use current stable registry names (`Transform`, not the illustrative `TransformComponent`) and real complete metadata properties.
- The initial on-disk scenes observed were v3 (`test.scene`, `_hotreload_temp.scene`) and v1 (`output.scene`). No automatic legacy data conversion is authorized by this observation.
- ET-16 must settle the OS/filesystem atomic replacement guarantee before saving to the real asset destination.

## ET-10 attach contract approval

- The user's "構いません" approves prevalidation followed by a non-failing OnAttach commit. All recoverable property, reference, hierarchy and UI failures must occur before publication/attachment. Exceptions and reentrant structural mutation during attach violate this contract; they are not recoverable materialization failures. This supersedes the unanswered-decision notes at the historical checkpoint above.
- Preparation uses an unpublished ordinary Scene and its existing ActorPool/GUID lookup, with final handles planned by the destination ActorPool. Actor addresses, GUIDs and handles survive ownership transfer. Component reference resolution must retain ActorReference/Actor identity, not the temporary Scene address; system registration belongs to OnAttach. No new resolver or runtime Actor graph is introduced.
- UI validation shares Scene's hierarchy rules but rejects Transform-family conversion for definition-owned structure. Candidate disposal does not invoke lifecycle callbacks for actors that never attached.

## ET-12 completion review (2026-09-12)

- Delivered the strict persistent Instance record for asset GUID, source DefinitionRevision, root GUID, nullable external-parent GUID, LocalObjectID-to-Actor-GUID mappings and optional Property Overrides. Reader and Writer are atomic, reject unknown/missing fields, invalid identities, duplicate mappings and non-standard JSON values, and sort mappings, targets and JSON Pointer keys deterministically. Empty Override targets normalize away while explicit `null` remains a value.
- Property diff uses the existing Reflection metadata and normalized default/current objects. Registered leaf paths are the comparison authority; arrays and typed references remain atomic leaf values and floating-point comparison is exact. One shared revision-independent validator owns normalized RFC 6901 paths, duplicate and ancestor/descendant conflicts, finite standard JSON and strict UTF-8 encoding.
- Restore first merges Overrides into each complete normalized default and then runs the existing Reflection deserializer, domain validators, type invariants and reference/asset validation on unpublished candidates. Same-revision missing targets, paths and incompatible representations are fatal. Different revisions drop only deleted targets/paths and incompatible outer representations; compatible malformed references, arrays, assets and invariant failures remain fatal and publish nothing.
- GUID migration preserves the intersection by LocalObjectID, generates collision-free GUIDs for added Actors and drops deleted mappings. The current root LocalObjectID must still map to the saved root GUID and is never auto-replaced. A successfully restored runtime Registry records the current definition revision, so the next save contains no stale entries.
- Instance serialization reads live Actor and Component values, validates serializable property domains and whole-object invariants, validates Actor and Asset references through existing contexts, and emits only changed leaves. Internal Actor references use typed local LocalObjectIDs; external references use typed Scene Actor GUIDs. Scene-scope encoding of an internal GUID is rejected so migration cannot bypass LocalObjectID identity.
- The user approved a batch-lifetime composite lookup for commit-before validation. SceneActorBatch installs a private candidate-to-destination fallback: candidate GUIDs/handles win, and unresolved lookups read the mutation-blocked destination. This supports references to ordinary Actors and Actors in another Instance before commit while preserving ET-10 atomic publication. The cached destination handle remains valid after the candidate Actors transfer. Missing or destroying targets fail without publication.
- Instance snapshots reuse the same validated record and RestoreInstance path, retaining root/child GUIDs, local references, properties, Transform state and ordinary external parent identity. Invalid or destroying Instances cannot replace a captured record.
- Focused record and persistence tests cover strict schema, embedded-NUL GUIDs, non-finite/binary/invalid-UTF-8 values, pointer escaping/conflicts, exact diff, null/array/reference forms, save validation, same/different-revision failure boundaries, stale removal, surviving/added/deleted GUIDs, root mismatch, external-parent policy, post-Stage Asset failures, Snapshot restore, external ordinary/other-Instance lookup and failure atomicity.
- `cmake --build build --config Debug --parallel 4` passed for Framework, GameCode, Game, Editor and all tests. `ctest --test-dir build -C Debug --output-on-failure` passed 39/39 tests in 5.64 seconds; the focused persistence test also passed. `git diff --check` passed. Existing MSB8064 and third-party DirectXTex missing-PDB warnings remain.
- Architecture review found no second Property system, Component serializer, Asset resolver, Actor identity store or public Scene resolver abstraction. Runtime ActorReference remains GUID/handle based, ActorPool remains the Actor owner, the Instance Registry remains provenance/correspondence only, and the composite lookup is private temporary transaction state. Scene v4 orchestration, live reload and Editor override UI remain with ET-13, ET-14 and ET-16/17. ET-12 meets its acceptance criteria; no Issue was closed.

## Completion review format

### ET-13 completion review (2026-09-12)

- Delivered Scene v4 with ordinary Actors and ActorImprint Instance records in separate sections. The Writer omits Imprint-owned Actors from the ordinary Actor array, orders ordinary Actors and Instances by GUID, rejects non-finite directional-light values, and produces deterministic Save-Load-Save output. The Loader accepts existing v3 Scenes, explicitly rejects v2, and refuses v3 input that contains non-empty v4 Instance data rather than silently dropping it.
- `SceneLoader::LoadCandidate` now returns a typed `SceneLoadResult` containing either a complete unpublished Scene or one located diagnostic. File loading parses exactly once, rejects duplicate JSON object keys, and validates strict wrapper fields, numeric ranges, global Actor GUID provenance, root mappings, hierarchy, Actor and Asset references, UI constraints, settings, and Instance Registry state before attachment. Actor and Instance input order does not affect construction or serialized output.
- One Scene-wide unpublished candidate owns every ordinary Actor and restored Instance until validation succeeds. Per-Instance publication validation and attachment are deferred during this transaction; all shells, hierarchy, overrides, cross-Actor references and UI rules are checked globally. Candidate disposal destroys unpublished Actors without lifecycle callbacks and releases definition pins only after their objects are gone. Storage needed by the non-failing attachment stage is reserved before callbacks begin.
- Definition-owned Imprint Transform structure is validated exactly during UI checks. It is never converted to another Transform family. A failure therefore rejects the candidate before `OnAttach`, leaves no callback side effect, and releases all candidate definition pins. Generated migration GUIDs are reserved against every GUID found in the persisted Scene, including records that occur later in the input.
- EditorApp, SceneManager, SceneCloner and hot-reload restoration use the candidate contract and publish only on success. EditorApp applies the saved viewport to the candidate before publication, then clears the old document state and swaps Scenes. SceneManager preserves the current Scene, name and viewport on load failure, suppresses requests made during prior-Scene teardown, and does not retry a missing Scene name every frame. Game tolerates an initial load failure and a valid Scene with no camera.
- Tests cover strict v4 parsing, duplicate fields, version/range failures, the repository's real v3 Scene schema, v2 rejection, global provenance collisions, root mismatch, reordered input, mutual ordinary/Instance references, migration, exact override diagnostic paths, hierarchy and UI failures, callback and pin cleanup, byte-equivalent round trips, SceneCloner, and SceneManager success/failure/teardown behavior. `cmake --build build --config Debug --parallel 4` passed for Framework, GameCode, Game, Editor and all test targets. `ctest --test-dir build -C Debug --output-on-failure` passed 41/41 tests in 5.16 seconds; `git diff --check` passed. Existing MSB8064 and third-party DirectXTex missing-PDB warnings remain.
- ActorPool remains the Actor identity and ownership store, Scene remains the hierarchy/lifecycle owner, the ActorImprint Registry remains provenance and LocalObjectID correspondence only, and Reflection/ComponentRegistry remain the property and construction boundaries. The loader adds orchestration and diagnostics; it does not add a second runtime graph, resolver, Component serializer or persistent identity system.
- The DOM overload cannot discover duplicate object keys already collapsed by the caller's JSON parser; the file overload provides that guarantee for persisted Scenes. `OnAttach` observes the complete persisted Actor graph and Instance Registry, while caller-owned state such as SceneManager identity and Editor viewport is applied around publication by each caller. Exceptions and reentrant structural mutation during callbacks remain contract violations under the ET-10 approval.
- EditorApp's failure ordering was reviewed directly: selection, command history, canvas context, viewport and the active Scene are mutated only after a successful candidate result. The test suite exercises the underlying Loader and SceneManager transactions, but there is no automated full EditorApp integration harness that initializes its Windows/D3D/UI state; this UI-level acceptance point remains code-review evidence rather than an automated test. No testing-only public API or broad Editor decomposition was added for ET-13.
- ET-14 owns live definition reload, and ET-15 owns the full Editor Document state model. No Issue was closed; final acceptance remains with the user.

### ET-14 completion review (2026-09-12)

- Delivered a typed System reload result and an all-live-Scene transaction. The System fully validates the candidate definition, verifies that the supplied Scene owners cover every pinned Instance, captures the current live overrides against the old defaults, builds every replacement Scene without lifecycle callbacks, and only then swaps the immutable definition and all affected Scene owners. The stable ActorImprintHandle remains unchanged.
- Candidate construction sees the new definition through a private resolver while public `Resolve` continues to expose the old published definition. Existing ET-12 migration preserves Actor GUIDs for retained LocalObjectIDs, generates collision-free GUIDs for additions, drops deleted IDs, reapplies valid overrides and takes new defaults for unoverridden values. A failed candidate releases all temporary Scene and definition pins without attaching Components or changing a live Scene, Registry, revision, selection or history.
- Successful results retain the displaced Scenes and old definition until the caller clears pointer-bearing Editor history, then finalize them explicitly. EditorApp processes changes after `EditorUpdate`, defers while an Inspector/Transform edit is active, clears history only for a replaced Edit Scene, re-resolves hierarchy selection and Canvas targets by GUID, and releases retired Scenes after those owner-specific updates.
- Editor Play mode freezes both definition reload and catalog refresh. Filesystem time is accumulated during Play so the first eligible Edit safe point promptly refreshes and processes queued changes. Runtime Game continues to use the definitions loaded with its Scene and has no live asset reload path.
- Removed or wrong-type catalog entries retain the loaded definition and live Instances but mark the handle Missing. Missing rejects new Load/Instantiate and public Scene save. A deleted/type-lost event is latched across deferred dispatch, so `Removed -> same-GUID broken Added` cannot accidentally make the old definition Available; only a completely validated same-GUID no-op or all-Scene reload clears Missing. Same-revision valid candidates are no-ops.
- `ActorImprintReloadTests` covers two-Scene commit, retained overrides and GUIDs, new defaults, additions/removals, source revisions, second-Scene late failure, callback-free rollback, temporary pin cleanup, incomplete Scene enumeration, Removed/Missing behavior, corrupt and valid same-GUID restoration, deferred Removed/Added corruption, and same-revision no-op. Existing Asset queue and Inspector transaction tests cover transition coalescing and active-edit detection.
- The full Debug build passed for Framework, GameCode, Game, Editor and all test targets. `ctest --test-dir build -C Debug --output-on-failure` passed 42/42 tests in 6.28 seconds; `git diff --check` passed. Existing MSB8064 dependency and DirectXTex missing-PDB warnings remain.
- There is no automated full EditorApp harness for Windows/D3D/ImGui interaction. The Play/Edit dispatch, success-only history clearing and GUID re-resolution are supported by direct code review plus a successful Editor build; UI smoke coverage remains for ET-18. No Issue was closed and final acceptance remains with the user.

### ET-15 completion review (2026-09-12)

- Added `IEditorDocument` as the shared editing-unit boundary and `EditorDocumentManager` as its owner. Each Document owns its Working Scene, Command History, GUID-backed Selection, Viewport Context, dirty state and display identity. `SceneEditorDocument` is the normal Scene adapter and owns Scene finalization and persistence.
- Moved the editor camera, Scene/Canvas mode, Canvas edit target, pan and zoom state from `EditorApp` and `SceneViewPanel` into `EditorViewportContext`. Moved the selected Actor GUID from `HierarchyPanel` into `EditorSelection`. Hierarchy, Inspector, Scene/Canvas View, MenuBar and Toolbar now consume the active Document context; shared Panels no longer retain the active Document's persistent selection or view mode.
- Routed successful commands, recorded property edits, Undo and Redo through the active Document so dirty state changes with the edited unit. Successful Save marks that Document clean. Dirty close returns explicit Cancel, Save-failed or Closed results, and a failed Save leaves the Document open.
- New Scene, candidate Scene load, save, GameCode hot-reload reconstruction and Play cloning now resolve their Scene through the Document boundary. Play checks the active Document capability, so a future ActorImprint Document cannot enter Play. The manager can own and activate a second Document implementation without adding a concrete-type branch to shared Panels.
- ActorImprint reload enumeration is contained by `EditorDocumentManager`: it supplies all live Working Scene owners for the synchronous System transaction, clears only affected histories, revalidates each affected Selection and Canvas target by GUID, marks changed Documents dirty, and returns the retained reload result so `EditorApp` can clear its render cache before finalizing displaced Scenes. Mutable Scene owner slots are private to the manager.
- `EditorDocumentTests` covers normal Scene save and clean transition, command-driven dirty state, active switching, independent history/selection/camera/mode/Canvas navigation/dirty state, non-Scene Play refusal, dirty close Save/Discard/Cancel/failure behavior, and affected-only state reconciliation after Scene replacement. Existing Editor command, camera, Inspector and ET-14 reload tests also pass.
- The final Debug build passed for Framework, GameCode, Game, Editor and all test targets. `ctest --test-dir build -C Debug --output-on-failure` passed 43/43 tests in 5.34 seconds; `git diff --check` passed apart from the repository's existing LF-to-CRLF notices. Existing MSB8064 dependency warnings remain.
- Basic manager activation and correct Panel context are present. Advanced tab appearance, OS windows, simultaneous Viewports and per-Viewport RenderTargets remain out of scope. The Windows/D3D/ImGui workflow still has no automated full EditorApp harness; ET-18 retains the UI smoke check. No Issue was closed and final acceptance remains with the user.

### ET-16 completion review (2026-09-12)

- Added an ActorImprint Editing Context and Document. Opening resolves one immutable definition from the System, expands it into an ordinary Working Scene, and builds a bidirectional LocalObjectID map. The Working Scene never becomes an ActorImprint runtime Instance and does not add Registry provenance.
- The Scene-owned authoring policy preserves exactly one closed root subtree. It rejects root deletion, a second root, moving the root under another Actor, promoting a child to a second root, unregistered Component types, and Actor references outside the Working Scene. Query and execution paths use the same typed result. No Main Camera or gameplay update is required.
- Existing Actor/Component/property commands are reused behind one `ActorImprintEditingCommand`. Scene mutation and ObjectMap reconciliation form one history unit. Undo/Redo restores LocalObjectIDs by Actor GUID and Component owner/type/occurrence while the next-ID watermark never decreases or reuses deleted IDs.
- Snapshot creation serializes Actor and registered Component state through the existing Reflection metadata, definition-local Actor reference codec and Asset catalog context. It validates the generated object with the ET-08 reader and publishes an immutable canonical definition. The minimum new-asset model uses the same path and contains one root Actor plus its required Transform with IDs 1 and 2.
- DefinitionRevision is regenerated only when normalized definition content or LocalObjectID state changes. Equivalent saves, including an edit reverted to the last saved state, retain the prior revision.
- The user approved a Windows local fixed NTFS/ReFS save contract. The implementation rejects other destinations before writing, creates the temporary and backup beside the asset, writes the complete temporary with `FILE_FLAG_WRITE_THROUGH`, calls `FlushFileBuffers`, validates Serialize-Deserialize-Serialize equality, and commits with backup-enabled `ReplaceFileW`. A failed replacement restores the original from the backup where Windows created one; no Asset notification, System definition, Instance Registry or live Scene is changed before filesystem commit. Power-loss durability is explicitly outside this contract. `ReplaceFileW` same-volume and flag behavior follows the [Microsoft API contract](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-replacefilew).
- A successful filesystem commit queues one content-only Modified event without rescanning or republishing the catalog. EditorApp consumes it at the existing ET-14 safe point. A successful Document save marks only that Document clean; failure preserves dirty state.
- `ActorImprintEditingTests` covers definition expansion, no runtime provenance, root and external-reference policy, reused structural commands, Undo/Redo identity stability, default root creation, ET-08 round-trip, AssetGUID document deduplication, Play refusal, successful save and ET-14 reload, equivalent revision retention, and a sharing-lock replacement failure that preserves the original bytes, dirty state, loaded definition and notification queue with no transaction residue.
- A serial full Debug `ALL_BUILD` passed for Framework, GameCode, Game, Editor and all test targets. The final `ctest --test-dir build -C Debug --output-on-failure` passed 44/44 tests in 5.62 seconds; `git diff --check` passed apart from line-ending notices. Existing MSB8064 dependency and third-party DirectXTex PDB warnings remain.
- Asset-panel creation/deletion, drag payloads, Instance placement, typed pickers and visible Document workflow belong to ET-17. Full Windows/D3D/ImGui smoke testing remains with ET-18. No Issue was closed and final acceptance remains with the user.

### ET-17 completion review (2026-09-12)

- Added an `Actor Imprints` panel that lists catalogued ActorImprint assets and emits only Create, Edit, Delete and GUID-plus-AssetType drag intents. The panel does not scan files, construct definitions, materialize Actors or own Document state. It shares the bottom dock with Scripts and remains disabled outside Edit mode.
- `ActorImprintAssetWorkflow` owns filename validation and asset lifecycle. Create accepts one filename, canonicalizes `.imprint`, rejects Windows-invalid/reserved names and case-insensitive collisions, builds the ET-16 one-root default model, validates it through the ET-08 reader, writes and flushes same-directory staging files, publishes the metadata before the discoverable asset file, and rolls both back if catalog publication fails. AssetGUID exists only in `.meta` and the catalog.
- Delete validates the catalog/type and `.meta` identity, refuses an open source Document or any live loaded Scene Instance, stages both files out of discoverable names, publishes one catalog removal, restores the pair if that publication fails, then removes the staging files. Rejection is diagnostic and leaves the pair and catalog unchanged.
- Edit opens the ET-16 ActorImprint Document and reuses manager-level AssetGUID deduplication. The default root, initial DefinitionRevision and IDs 1/2 with next ID 3 come from the normal editing snapshot path rather than panel-specific JSON.
- Hierarchy drag uses one trivially-copyable payload containing only `AssetGUID + AssetType`. Drops are offered on the Scene root and Actor nodes; the command accepts only a normal Scene Document and validates the external parent against ET-11. Instance roots and members are rejected, and there is no path, pointer, Handle or automatic Transform adjustment in the payload or placement path.
- `InstantiateActorImprintCommand` uses ET-10 materialization for its first execution and captures the ET-12 persistent Instance snapshot. Undo destroys the complete Instance through the System/Scene boundary; after deferred collection, Redo restores the same root/member ActorGUID mapping and external parent. Failed Load, policy or materialization paths are never recorded in history.
- The existing reflection Inspector's generic AssetPicker now accepts the same typed payload. Its type and catalog checks make `AssetReference<ActorImprint>` selection use the same path as other reflected AssetReference properties; no ActorImprint-specific property editor was introduced.
- `ActorImprintWorkflowTests` exercises extension/name rules, case-insensitive collision, pair/catalog publication, default content, typed picker rejection, Document open/deduplication, both deletion constraints, ordinary-parent placement, Instance-target rejection, missing-asset failure, success-only history, whole-Instance Undo, GUID-preserving Redo and final pair/catalog deletion. The serial Debug `ALL_BUILD` passed and full CTest passed 45/45 in 6.97 seconds. `101Editor` linked with the new panel/workflow sources. Existing MSB8064 dependency and DirectXTex missing-PDB warnings remain.
- Subtree-to-Imprint conversion, automatic placement offsets, nested Instances, unpacking, variants, override visualization and advanced rename/move workflows remain outside this ticket. ET-18 owns the final cross-ticket traceability, E2E, smoke, failure and performance audit. No Issue was closed and final acceptance remains with the user.

### ET-18 completion review (2026-09-12)

- Added [`ActorImprintTraceability.md`](ActorImprintTraceability.md), mapping the Final Design's U-01 through U-12 responsibility areas to owning tickets, production code and primary verification. It also records the E2E acceptance matrix, failure-preservation evidence, ownership/dependency audit and explicit limitations.
- Added `ActorImprintEndToEndTests`, which executes Create -> Edit -> Save -> two Editor command Instances -> Property Overrides -> Scene v4 Save -> fresh AssetManager/System Load -> deterministic re-save -> two-Document Definition reload -> Undo-history boundary -> corrupt reload rejection -> deletion constraints. It also creates a third Instance through the game/runtime `AssetReference<ActorImprint>` API.
- The E2E graph has three hierarchy levels, two same-type reflected Components, stable LocalObjectIDs, definition-local ActorReference, typed AssetReference, cross-Instance ActorGUID reference, scalar and Actor-name overrides. A process-restart load verifies hierarchy, ActorGUID/AssetGUID identity, references and override/default behavior.
- Determinism evidence compares canonical JSON before and after restart and two independently persisted Windows files byte-for-byte. Scene v4 contains exactly two Instance records and does not repeat Instance members as ordinary Actors.
- Failure evidence spans ET-16 atomic save replacement, ET-10 materialization factory/Property/reference/exception failures, ET-13 late whole-Scene load failure and ET-14 late multi-Scene/corrupt-definition reload failures. Each test asserts the relevant published files, definition, ActorPool/GUID map, Registry, Scene owners, history or dirty state remains unchanged.
- Architecture review found one immutable definition source, one Scene-owned provenance Registry, one Asset catalog, one ActorPool/hierarchy owner and the existing Reflection/Component factory sources. Generic Actor/Component state has no Imprint membership or variant flag; UI is an intent route into the existing policy and transaction boundaries. No Framework-to-Editor dependency or second mutation/serialization model was introduced.
- The final Debug `ALL_BUILD` passed for 101Framework, GameCode, 101Game, 101Editor and all tests. Full CTest passed 46/46 in 6.00 seconds. A hidden 101Editor startup smoke remained alive for five seconds before the harness stopped its own process.
- The representative Debug observation instantiated 64 three-Actor Instances and serialized the Scene in 108 ms. This is an observation rather than a release benchmark; candidate preparation has the documented temporary memory cost proportional to Scene slots/indices plus candidate objects.
- Limits remain explicit in the traceability document: current single-threaded operation, fixed-local-filesystem save contract, registered-type lifetime, borrowed-pointer invalidation on Scene replacement, and the lack of scripted ImGui interaction. Deferred feature candidates remain outside ET-08 through ET-18.
- No material unresolved design decision remains. ET-08 through ET-18 satisfy their recorded acceptance evidence and are presented together as completion candidates. No Issue was closed and final acceptance remains with the user.

### Issue #68 completion review (2026-09-13)

- Added a central `DocumentTabBar` above the existing Scene/Canvas viewport modes. It enumerates manager-owned Documents as value snapshots and shows display name, active state, dirty marker and close action without taking ownership of WorkingScene, history, selection, viewport or dirty state.
- Added `EditorDocumentWorkflow` as the application-level close/exit decision coordinator. Dirty close and Editor exit use the same Save/Discard/Cancel policy; cancellation and save failure preserve open Documents and state. Exit saves remain non-destructive until every dirty Document is resolved, and the workflow rechecks Documents after save-side reload processing.
- Routed tab activation/close back into `EditorDocumentManager`, so the existing active-Document accessors continue to supply Hierarchy, Inspector, viewport, Save, Undo and Redo. The existing single render target remains active-Document-only; no per-Document render target or simultaneous viewport was introduced.
- `ActorImprintAssetWorkflow::Create` now publishes under `asset/ActorImprints/`, preparing the directory before the existing two-file transaction. `ActorImprintsPanel` filters the catalog to that managed directory and provides empty-space Create, item Edit/Delete, and double-click Edit. `Assets > Create Actor Imprint...` opens the same panel modal and callback route.
- Repository inspection found no existing `.imprint` asset to migrate. Compatibility is explicit: root-level typed catalog entries remain loadable by existing GUID paths but are omitted from this managed panel. Tests create such a legacy fixture to prove the filter without rewriting data.
- `EditorDocumentTests` drives tab activation/close intents through production callbacks and covers multi-Document exit Save/Discard/Cancel/failure preservation. `ActorImprintWorkflowTests` drives MenuBar/panel Create/Edit/Delete intents through production callbacks, verifies managed-directory filtering and directory-preparation rollback. E2E paths were updated to the managed directory.
- Final Debug `ALL_BUILD` passed for Framework, GameCode, Game, Editor and every test target. Full CTest passed 46/46 in 11.91 seconds. A hidden `101Editor` startup smoke remained alive for five seconds before the harness stopped its own process. The existing MSB8064 dependency warning remains.
- No runtime ActorImprint schema, LocalObjectID, DefinitionRevision, reload transaction or structural-policy boundary changed. Advanced tab ordering/pinning/splitting, multiple OS windows and simultaneous Viewports remain out of scope. No Issue was closed and final acceptance remains with the user.

### ET-11 structural/lifetime pilot checkpoint

- Added a single Scene StructuralMutationResult/Can* boundary, routed Actor Add/Destroy and direct Component destruction through it, restricted Actor identity/ownership and destruction callbacks to engine friends, and implemented validated Instance destruction with a Destroying record and deferred reverse-index cleanup.
- The ET-11 pilot within ActorImprintMaterializationTests passed (suite 2.78 seconds) before expansion. It checks matching query/execution rejection reasons, member deletion/reparent/Component mutation refusal, whole-root destruction, immediate Component correspondence invalidation, GUID/ActorHandle membership retention until GC, and final pin release.
- Component teardown previously skipped OnDestroyOverride after MarkForDestruction. A separate lifecycle notification flag now allows the existing deferred removal path to notify exactly once. It is not an Imprint membership flag.
- Expansion after this checkpoint covered indirect UI-driven Transform conversion prevalidation, ordinary-Actor cascade handling, reason propagation through existing Editor Commands, public snapshot/restorer bypass checks, and the full mutation matrix described below.

### ET-11 completion review (2026-09-12)

- Delivered one Scene-owned structural policy boundary for Actor insertion, reparenting and destruction, Component mutation, Transform replacement and Canvas render-mode transitions. Query and execution paths now return the same typed refusal reason, and Editor commands/history retain that reason for review.
- An Imprint member remains structurally immutable. The instance root is the only direct destruction entry, and whole-instance provenance, Actor GUID reservations, handles and reverse indices remain valid through deferred destruction until physical collection. Ordinary subtree destruction treats every included Imprint instance as an indivisible unit.
- Lifecycle callbacks run inside the Scene structural transaction gate, with notification state set before user overrides. Pending Component attachment is published to stable Actor storage before callbacks. Reentrant Actor or Component mutation is rejected without invalidating ActorPool or Component container iteration, and detach/destroy callbacks run exactly once.
- UI hierarchy transitions use the same validated commit path. Non-cascade Actor removal reparents children through UI-aware logic, while a deferred Canvas becomes logically absent at mark time so later validation sees the post-removal hierarchy. Imprint Transform structure also requires the exact registered Transform type, rather than only matching TransformKind.
- Snapshot/restorer and direct mutation bypasses are rejected through the same policy. ComponentSnapshot propagates refusal reasons, and EditorCommandHistory preserves the last structural result after a failed command object is discarded.
- `cmake --build build --config Debug --parallel 4` passed for Framework, GameCode, Game, Editor and all test targets. `ctest --test-dir build -C Debug --output-on-failure` passed 37/37 tests in 4.81 seconds; the focused ActorImprintStructuralMutationTests also passed. `git diff --check` passed. Existing MSB8064, DirectXTex PDB and Renderer/CollisionSystem C4819 warnings remain.
- Architecture review found no second hierarchy, lifecycle owner, Component policy or Instance identity store. ET-12 record/override persistence, ET-13 whole-Scene loading, ET-14 reload, and ET-17 workflow history remain in their owning tickets. ET-11 meets its acceptance criteria; no Issue was closed.

### ET-10 completion review (2026-09-12)

- Delivered typed-asset/Handle Instantiate, GUID-preserving RestoreInstance, optional diagnostics, shared candidate preparation and publication, Scene-owned Instance Registry, type/occurrence Component correspondence, definition lifetime pins and post-GC membership cleanup.
- Full Debug build passed for Framework, GameCode, Game, Editor and tests. All 36 tests passed in 5.53 seconds, including the production materialization suite (2.78 seconds). Existing MSB8064/DirectXTex PDB warnings remain. The full rebuild also reports pre-existing C4819 warnings in Renderer/CollisionSystem.
- Expanded evidence: independently owned Instances, built-in Camera and custom reference resolution, final-handle stability across transfer, complete Registry visibility during attach, ordinary external parent/root, forbidden Instance parents, malformed restored GUIDs, failure injection in factory/property/reference resolution, exceptions, free-slot/generation preservation, Canvas structural incompatibility, invalid UIImage target, unavailable/missing Asset references, multiple Scene lifetime pins, implicit Imprint Scene teardown, and a reversed 2,048-Actor hierarchy.
- Two verification defects were corrected: integer JSON in a float-only test property was changed to its actual serialized representation; automatic Finalize is limited to Scenes retaining Imprint records to preserve existing ordinary-Scene teardown behavior. The regression suite then passed. No general ordinary-Scene lifetime migration was included.
- Registry owns provenance only; ActorPool owns Actors, Components remain Actor-owned, existing Reflection and Component ResolveReferences own value/reference behavior. Private SceneActorBatch transfers the existing objects with unchanged GUIDs/handles/addresses. Candidate callbacks must not retain the temporary Scene address. Non-failing attach and reentrant structural restrictions follow the user's explicit approval.
- Neighboring consumers: ET-11 owns complete public structural policy and Destroying state; ET-12 owns overrides and record persistence; ET-13 owns whole-Scene staging and caller replacement; ET-14 owns all-live-Scene replacement; ET-17 owns history snapshots/Undo. Their functionality was not implemented early. The candidate storage cost is documented at the pilot checkpoint below. No second pool implementation, factory, property schema, resolver or hierarchy model was introduced.
- Acceptance criteria are satisfied for ET-10; no Issue was closed and final acceptance remains with the user.

### ET-10 publication pilot checkpoint

- The production Instantiate/RestoreInstance path now uses detached preparation, an unpublished ordinary Scene, planned final ActorHandles, complete reference/UI validation, destination storage preparation, ActorPool/GUID/Registry publication, then non-failing attach. The pilot test passed (0.08 seconds) before expanding coverage.
- Pilot evidence covers one real Transform Actor, provenance and Component lookup, failed GUID-conflicting restore preserving state, definition unload/clear refusal while referenced, and Scene teardown releasing the pin after Actor collection.
- API review: callers provide a typed asset or handle and optional external parent/diagnostics. Registry uses type + occurrence to reacquire Components. SceneActorBatch is a private construction facility for System/SceneLoader, not an alternative Actor pool or resolver. The later whole-scene loader can build on the same preparation phases without exposing a partially loaded destination.
- Temporary memory includes candidate ActorPool slots up to the highest planned destination index and copies of destination GUID/Registry indices. This straightforward transactional implementation has O(destination slots + existing index entries + candidate objects) temporary memory; it is not a constant-cost spawn path. No general pooling or speculative optimization was added.

For each ticket record actual code and test evidence, ownership/dependency decisions, known neighboring consumers, abstractions and duplication added/removed, pilot checkpoint, deferred issues and unresolved limitations. At integration points review the resulting subsystem as well as the diff. No issue is closed or finally accepted by the implementer.
